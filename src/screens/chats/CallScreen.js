import {View, Text} from 'react-native';
import React from 'react';

const CallScreen = () => {
  return (
    <View style={{alignItems: 'center', flex: 1}}>
      <Text>We will connecting with call packages </Text>
    </View>
  );
};

export default CallScreen;
